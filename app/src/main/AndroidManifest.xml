<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.rup.alternative">

    <supports-screens
        android:largeScreens="true"
        android:smallScreens="true" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Pre Writing Skills"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".match"></activity>
        <activity android:name=".Intial">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".first" />
        <activity android:name=".second" />
        <activity android:name=".third" />
        <activity android:name=".fourth" />
        <activity android:name=".fifth" />
        <activity android:name=".sixth" />
        <activity android:name=".lfirst" />
        <activity android:name=".lsecond" />
        <activity android:name=".lthird" />
    </application>

</manifest>